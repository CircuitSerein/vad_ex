# Elixir / Mix
/_build/
/cover/
/deps/
/doc/
/.fetch
erl_crash.dump
*.ez
*.beam
/config/*.secret.exs
.elixir_ls/

# Rust NIF
/native/vad_ex/target/
# Locally-built NIF artifact (VAD_EX_BUILD=1); shipped via GitHub release assets, not git.
/priv/native/

# Real-audio test corpus: raw downloads + decoded PCM are fetched on demand, never committed
# (LibriSpeech/MUSAN are large + CC BY). Run test/corpus/fetch_corpus.sh to populate.
# Only the derived golden vectors + manifest + fetch script are tracked.
/test/corpus/.cache/
/test/corpus/data/
/test/corpus/.venv/

# Env / secrets — never in git
.env
.env.*
!.env.example

# --- private docs: client comms, requirements, version requests — NEVER in git ---
# Rides Syncthing between machines, not git. See library/_skeleton-docs/STANDARD-docs.md
_private/
